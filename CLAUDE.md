# ضيوف — دليل العمل في هذا المستودع

منصة SaaS فندقية متعددة المنشآت. FastAPI + PostgreSQL، بلا ORM —
استعلامات SQL مُمعلَمة مباشرة.

---

## التشغيل والاختبار

```bash
pip install -r requirements.txt
python3 -m pytest tests/ -q --ignore=tests/e2e   # ١١٩ اختباراً
ruff check . --select=E,F,W --ignore=E501
uvicorn main:app --reload --port 5050
```

اختبارات RLS تحتاج PostgreSQL حقيقياً وتُتخطّى بدونه:

```bash
TEST_DATABASE_URL="postgresql://postgres@localhost/test_db" \
  pytest tests/test_rls_isolation.py tests/test_rls_end_to_end.py -q
```

**لا تُحاكِ RLS.** محاكاتها تُثبت أن المحاكاة صحيحة لا أن السياسة صحيحة.

---

## البنية

```
main.py           نقطة الدخول — يُعيد تصدير app من app_core
app_core.py       التطبيق، الجلسات، كلمات المرور، middleware، تركيب المسارات
routes/*.py       ٢٩ وحدة، كلٌّ يُصدّر router
db/connection.py  مجمَّع الاتصالات + ربط سياق المستأجر
db/store.py       طبقة البيانات (PostgreSQL + مخزن JSON للتطوير)
db/rls.py         سياسات العزل في قاعدة البيانات
db/tenant_context.py  ContextVar لمستأجر الطلب
services/*.py     منطق المجال
static/dashboard.html  لوحة التحكم كاملةً (HTML+JS في ملف واحد)
```

---

## قواعد لا تُخالَف

### ١ — العزل بين المنشآت

**كل استعلام على جدول يحمل `client_id` يجب أن يُصفّى به.** يحرس ذلك
`tests/test_tenant_isolation.py` بفحص AST لكل استعلام في `routes/`.

هوية المستأجر **من الجلسة على الخادم دائماً** — لا من مسار ولا من جسم
طلب. مسارات مثل `/api/pricing/rules/{client_id}` تتجاهل المعرّف عمداً
وتستخدم الجلسة؛ لا تُصلحها لتستخدمه.

### ٢ — التحقق بالكسر لا بالمرور

اختبارٌ يمرّ ليس دليلاً على أنه يفحص شيئاً. قبل الوثوق بأي حارس، اكسر
ما يُفترض أن يكشفه وتأكّد أنه فشل. هذا المستودع فيه سابقة: RLS كان
يبدو مُفعَّلاً وهو صفرٌ عملياً.

### ٣ — لا سقوط صامت

فشل قاعدة البيانات يوقف الإقلاع، ولا يتحوّل إلى مخزن JSON إلا بـ
`ALLOW_JSON_FALLBACK=1` للتطوير المحلي.

---

## مطبّات تكلّفت اكتشافها

**`_hash_password` افتراضها ١٠٠ ألف دورة عمداً.** مسارات المشرف تقارن
ناتجها بتجزئات قديمة؛ تغيير الافتراضي يكسر دخول المشرف صامتاً. للتجزئة
الجديدة استعمل `_make_password` (٦٠٠ ألف، صيغة مُوسَّمة).

**كلمة المرور والملح يُكتبان معاً دائماً.** كتابة `pass_hash` وحده
تقفل المنشأة نهائياً — التحقق يستخدم ملح الحساب فلا يُطابق أبداً.

**`pass_hash` مخزَّن داخل `clients.settings._account`.** لا تُعِد كتلة
`settings` كما هي؛ استعمل `db.store.public_settings()` أو
`public_client()`.

**`apiFetch` في `dashboard.html` يبتلع كل خطأ ويعيد `null`.** لواجهة
تحتاج عرض رسائل الخادم استعمل `apiSend`.

**`loadSection` تُحلّ أسماء الدوال نصّاً.** لا تُعِدها إلى مراجع مباشرة:
دالة واحدة غير معرَّفة كانت تمنع **كل** قسم من التحميل.

**`run_in_executor` لا ينقل ContextVars.** أي عمل غير متزامن يلمس
قاعدة البيانات يحتاج `contextvars.copy_context()`، وإلا فقد سياق
المستأجر.

---

## حالة الأمان

| البند | الحالة |
|---|---|
| العزل في طبقة التطبيق | ✅ مُطبَّق ومحروس |
| RLS في قاعدة البيانات | 🟡 مكتوب ومُثبَت، **غير مُفعَّل** — `docs/RLS_DEPLOYMENT.md` |
| كلمات المرور | PBKDF2-SHA256 ٦٠٠ ألف دورة (لا Argon2 بعد) |
| حسابات الموظفين | ✅ سبعة أدوار، يُنشئها مالك المنشأة |
| تشفير أرقام الهوية | ❌ **غير موجود** — نصّ صريح ومفهرس |
| `CORS` / `ALLOWED_HOSTS` | ❓ لم يُفحصا |
| بوابة الدفع | ❌ لا تكامل خادم |

---

## هرم الصلاحيات

```
مالك المنصة  → كل شيء عبر كل المنشآت (جلسة admin_token منفصلة)
مالك المنشأة → كل شيء داخل منشأته + إنشاء المدير والموظفين
مدير عام     → مثلها، بحساب يمكن إيقافه
مدير مناوبة · استقبال · إشراف · محاسب · كاشير → كلٌّ بحدّ وظيفته
```

`owner` لا يُسنَد من الواجهة: إسناده يُنشئ مالكاً ثانياً لا يُوقَف.

---

## متغيّرات البيئة

| المتغيّر | الأثر |
|---|---|
| `DATABASE_URL` | إلزامي في الإنتاج |
| `ALLOW_JSON_FALLBACK` | تطوير محلي فقط |
| `RLS_ENABLED` | يُشغَّل بعد تطبيق السياسات ودور NOBYPASSRLS |
| `RLS_APPLY_ON_STARTUP` | مرة واحدة بمستخدم يملك الجداول |
| `ADMIN_PASSWORD` · `SECRET_KEY` · `PASS_SALT` | أسرار — من البيئة لا الكود |

---

## قبل الدفع

```bash
ruff check . --select=E,F,W --ignore=E501
python3 -m pytest tests/ -q --ignore=tests/e2e
```

الفرع: `claude/merge-hotel-system-repos-lc45bs` · `main` لا يُدفع إليه
مباشرةً.
