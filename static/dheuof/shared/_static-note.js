/* @jsx React.createElement */
/* global React */

/* =========================================================================
   Static (non-React) sidebar — used by stub-experience pages so we don't
   need to load Babel/React on every module. Render with vanilla HTML.
   Just include this file's CSS classes from shared/chrome.css and write the
   markup directly. See modules/02-shumus/index.html for the pattern.
   ========================================================================= */

// Kept for parity — actual sidebar in stubs is hand-rolled HTML.
