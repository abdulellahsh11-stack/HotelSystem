// fonts/ — drop your .woff2 / .ttf brand font files here.
// See README.md "Type substitution" — currently Google Fonts substitutes are
// wired up in ../colors_and_type.css (@import). To swap to local files:
//   1. Drop your files in this folder (e.g. ReemKufi.woff2, IBMPlexSansArabic.woff2)
//   2. Remove the @import url(...) line at the top of colors_and_type.css
//   3. Add @font-face declarations pointing to fonts/yourfile.woff2
