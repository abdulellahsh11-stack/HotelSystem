# تقرير الفحص والإصلاح — منصة ضيوف

> فحصٌ شامل لطبقة قاعدة البيانات والمسارات، وإصلاح ما وُجد.
> كل ما في هذا التقرير **مُثبَت بتشغيل فعلي** على PostgreSQL حيّة —
> لا باستنتاج من قراءة الكود.

| | البداية | الآن |
|---|---|---|
| الاختبارات | ٢٤ | **٢٨٢** |
| سياسات العزل | ٠ | **٦٩** |
| `client_id` إلزامي | ٠ عمود | **٦٦** |
| مسارات محروسة بصلاحية | ٠ | **١٦** |
| طرق العرض التقريرية | ١ | **١٠** |
| تعليقات داخل قاعدة البيانات | ٠ | **٣٨** |
| مواضع الكتابة في سجل المراجعة | ٠ | **كل عملية تغيير** |
| مسارات مُعطَّلة | ١٢ | **٠** |

١٧ التزاماً · ٧١ ملفاً · CI أخضر في وضعَي العزل.

---

## القسم الأول: أمانٌ كان موثَّقاً ولم يكن مُنفَّذاً

### ١ــ١ التحصين الأمني لم يكن يعمل إطلاقاً

مُقسِّم SQL كان يعتمد `split(";")` وفحصاً ساذجاً `startswith("--")` لتخطّي
التعليقات. معاً كانا يُسقطان **كل عبارة مسبوقة بتعليق توضيحي** — أي
معظم `04-isolation-hardening.sql` — بينما يُسجَّل في السجل
«✅ Security hardening» بنجاح كاذب.

المفارقة: كلما كان الملف أفضل توثيقاً سقط منه أكثر.

**١٩ من ٢٤ عبارة لم تكن تُنفَّذ.** الكائنات الغائبة عن قاعدة البيانات:

```
app_tenant · app_has_perm · app_branch_ok · app_actor_is_guest
mask_serial_id · audit_log_immutable · cleanup_revoked_sessions
staff_roles · branches · audit_log · secure_file_links
v_security_definer_audit
```

أي أن **دوال عزل المستأجرين وسجل المراجعة وصلاحيات الموظفين كانت غائبة**
رغم توثيقها كمُطبَّقة.

### ١ــ٢ العزل الصفّي كان صفراً

`RLS` مُفعَّل على ٧ جداول بصفر سياسات، والسياسة الوحيدة مكتوبة **كتعليق**
في `schema_v3.py:627`. وتفعيل RLS بلا سياسة يمنع كل الصفوف عن غير
المالك — لكن التطبيق يتصل بالمالك، والمالك يتجاوز RLS.

النتيجة: **عزل معدوم بمظهر مؤمَّن** — وهو أسوأ من غياب الحماية لأنه يُطمئن.

وعلّة ثانية صامتة: `set_event_context` كانت تضبط `app.tenant_id` محلياً
للمعاملة عبر `db.execute` الذي يُنفّذ `COMMIT` فوراً — فيُمحى الضبط قبل
أن يستفيد منه أي استعلام. **الدالة كانت بلا أثر إطلاقاً.**

### ١ــ٣ تجزئة كلمات المرور

| المصدر | ما كان يقوله |
|---|---|
| وثيقة التصميم §13.1 | «bcrypt cost 12» |
| `generate_dheuof_report.py` | «bcrypt-12» |
| `generate_erd.py` | `password_hash … bcrypt` |
| **الكود الفعلي** | **PBKDF2-SHA256، ١٠٠ ألف دورة** |

أضعف من توصية OWASP (٦٠٠ ألف لـ PBKDF2)، وليس ما يقوله التوثيق في ثلاثة
مواضع. والهاش كان `hex` عارياً بلا وسم خوارزمية، فلا سبيل لترقيته دون
إجبار الجميع على تغيير كلمات مرورهم.

### ١ــ٤ أرقام الهوية نصاً صريحاً

`guests.id_number` و `employees.national_id` و `iqama_number` — كلها
`VARCHAR(20)` صريحة. أي نسخة احتياطية مسرَّبة تكشفها، وهي بيانات يصنّفها
نظام حماية البيانات الشخصية السعودي حسّاسة.

### ١ــ٥ دخول مالك المنصة بلا حماية من التخمين

دخول المنشأة كان محميّاً، ودخول المالك — الحساب الذي تفتح كلمة مروره كل
المنشآت — بلا أي حدّ للمحاولات.

---

## القسم الثاني: وظائف كانت معطَّلة بالكامل

| الوظيفة | العلّة |
|---|---|
| **إضافة نزيل** | يُولّد معرّفاً سداسي عشري لعمود `SERIAL` فينهار تحويله |
| **منح نقاط الولاء** | `ON CONFLICT` على قيد فريد غير موجود — يفشل دائماً |
| **قائمة الموظفين** | `count_result[0]` على صف قاموس يرفع `KeyError: 0` |
| **حجوزات القنوات** | نفس العلّة |
| **مبيعات نقاط البيع** | نفس العلّة |
| **الفواتير المحاسبية** | نفس العلّة + `g.name` عمود غير موجود |
| **إنشاء الحجز** | `bookings.booking_number` مُشار إليه في ٤ ملفات ولم يوجد قط |
| **تسجيل الوصول والمغادرة** | نفس العلّة |
| **سجل مزامنة القنوات** | الجدول معرَّف مرتين بأعمدة مختلفة |
| **محرك التسعير الديناميكي** | `rooms.is_deleted` غير موجود |
| **تدقيق الليل** (٧ مسارات) | `_require_manager` يُعيد ٤٠٣ للمالك نفسه |
| **تعديل موظف وإنهاء خدمته** | مُشغّل يُسنِد `updated_at` والعمود غير موجود |
| **بقاء الجلسة** | كل نشر يُخرج جميع المستخدمين |
| **إعادة تعيين كلمة مرور منشأة** | تُخرجها من حسابها بصمت |

### تفصيل أهمّها

**إعادة تعيين كلمة المرور كانت تُخرج المنشأة من حسابها.** المسار يكتب
`pass_hash` بالملح العام ويترك `pass_salt` الخاص بالحساب. وعند الدخول
يتحقّق النظام بملح مختلف عن الذي جُزّئت به كلمة المرور — فتُرفض دائماً،
بلا أي رسالة خطأ تُفسّر ما جرى.

**كل جلسة تُستعاد كانت تُعتبر منتهية.** الجلسة في الذاكرة تُخزَّن بطابع
زمني بلا منطقة زمنية، والمُستعادة من PostgreSQL تحمل منطقة زمنية. والطرح
كان يتم دائماً مقابل `datetime.now()` المجرَّدة — وطرح وقتٍ يحمل منطقة
زمنية من وقتٍ لا يحملها يرفع `TypeError` يُلتقط في `except` ويُعيد
«منتهية». فجدول `client_sessions` — الموجود أصلاً لتبقى الجلسات بعد
إعادة التشغيل — **لم يكن يفي بغرضه إطلاقاً**.

---

## القسم الثالث: كيف اكتُشف نصفها

اختبار الدخان الأول **مرّ وهو أعمى**. كان يجمع المسارات من `app.routes`
مباشرةً، وFastAPI الحديثة لا تُسطّح الموجِّهات المُضمَّنة هناك بل تلفّ
كلاً منها في `_IncludedRouter` يحمل الموجِّه في `original_router`. فكان
يرى **١٩ مساراً من أصل ٩١**.

اكتُشف ذلك بإعادة علّة معروفة عمداً والتحقّق أن الاختبار يكشفها. لم
يكشفها — فكان الخلل في الاختبار لا في الكود. وبعد تصحيح الجمع ظهر
تعارض مخطط سجل القنوات.

**الدرس:** اختبارٌ يمرّ ليس دليلاً على أنه يفحص شيئاً. التحقّق الوحيد
المُعتبَر هو كسرُ ما يُفترض أن يكشفه.

---

## القسم الرابع: ما بُني

### العزل — ثماني طبقات

| الطبقة | الآلية |
|---|---|
| التطبيق | شرط `client_id` في كل استعلام |
| سياق الجلسة | `app.tenant_id` و`app.branch_ids` يُربطان بكل اتصال |
| قاعدة البيانات | ٦٩ سياسة عزل مستخرَجة من الكتالوج |
| الدور | `dheuof_app` بأقل امتيازات |
| طرق العرض | `security_invoker = true` |
| الفروع | قيد `branch_id` داخل نفس السياسة |
| الصلاحيات | `require_permission` على ١٦ مساراً |
| المخطط | `client_id` إلزامي في ٦٦ عموداً |

**الإثبات ليس وجود السياسات** بل اختبار يُنفّذ استعلاماً *بلا أي شرط*
`client_id` عبر مجمّع التطبيق نفسه، ويتحقّق أن الناتج يخصّ مستأجراً
واحداً. ومحاولة الكتابة باسم مستأجر آخر تُرفض بـ `InsufficientPrivilege`.

### الصلاحيات ودخول الموظفين

كانت هوية الموظف نصاً في جسم الطلب (`staff_name`) — أي أن أي مستخدم
ينسب أي عملية لأي موظف. سجلّ «من نظّف الغرفة» و«من أعطى الخصم» بلا قيمة
إثباتية.

| الحساب | المسار | الدور |
|---|---|---|
| المنشأة | `/api/login` | `owner` — صلاحية مطلقة |
| الموظف | `/api/staff/login` | دور مُسند |
| مالك المنصة | `/api/admin/login` | إدارة المنصة |

الصلاحيات متدرّجة بالنقطة: من يملك `rooms` يملك `rooms.read` ضمناً، فلا
يُنسى فرعٌ فيُترك مسار مفتوحاً أو مغلقاً خطأً. ورسالة فشل واحدة لكل
حالات الدخول — تمييزها يكشف الأرقام الوظيفية الموجودة.

### سجل المراجعة

الجدول كان موجوداً ومُحصَّناً ضد التعديل بمُشغّل، **وصفر موضع كتابة**.

التغطية الآن عبر وسيط HTTP لا استدعاءات متفرّقة: ما يمرّ عبر الشبكة
يُسجَّل بحكم الموقع، فلا يُنسى مسار جديد. وسياسة عزل خاصة — قراءة مقصورة
وإضافة مفتوحة — وإلا لضاع أثر عمليات مالك المنصة تحديداً لأنها بلا سياق
مستأجر. والأسرار تُنقَّى على العمق قبل الكتابة.

### تشفير أرقام الهوية

`AES-256-GCM` في طبقة التطبيق لا عبر `pgcrypto` — الأخير يوجب تمرير
المفتاح داخل نص كل استعلام، فينتهي إلى سجلّات الاستعلامات، أي إلى نفس
المكان الذي نحمي البيانات منه.

والبحث محفوظ عبر فهرس أعمى `HMAC-SHA256` يُطبّع الشرطات والفراغات
والأرقام العربية الهندية — فيجد البحثُ «١٠٩٨٧٦٥٤٣٢» و«1098-765-432» نفس
الصف دون فكّ تشفير أي صف.

### النسخ الاحتياطي

`verify` لا يقرأ حجم الملف — يستعيد النسخة فعلاً في قاعدة مؤقتة ويعدّ
جداولها وصفوفها ثم يحذفها. مُختبَر: ٧٠ جدولاً استُعيدت، ونسخة مبتورة
رُفضت برمز خروج غير صفري.

---

## القسم الخامس: اكتشافان عمليان

### المستخدم الخارق يتجاوز RLS حتى مع FORCE

سلوك PostgreSQL المقصود ولا يمكن تعطيله. اكتُشف حين مرّت اختبارات
الفروع وهي لا تفحص شيئاً.

**الأثر العملي:** إن كان مستخدم `DATABASE_URL` خارق الصلاحيات — وبعض
الخدمات المُدارة تُنشئه كذلك — فلن تسري أي سياسة عزل عليه مهما فُرضت.
الحماية تأتي من **تبديل الدور** لا من `FORCE` وحده.

```sql
SELECT rolsuper FROM pg_roles WHERE rolname = current_user;
```

إن عادت `t` فكل ضمانات العزل معلّقة على `RLS_ENFORCE=1`.

### أداة الحماية من الإساءة كانت مساراً للإساءة

`_login_attempts` قاموس لا يُنظَّف: كل عنوان رآه الخادم يبقى مفتاحاً إلى
الأبد. مُهاجم يُدوّر العناوين يُنمّيه حتى تنفد الذاكرة.

---

## القسم السادس: ما بقي

### قبل التسويق — خطوات تشغيلية

كلها متغيّرات بيئة وجدولة في لوحة المزوّد، لا يمكن ضبطها من الكود.

| # | البند |
|---|---|
| ١ | `RLS_ENFORCE=1` بعد تصفّح يدوي لكل وحدة |
| ٢ | `?sslmode=require` في `DATABASE_URL` |
| ٣ | توليد `PII_ENCRYPTION_KEY` و`PII_BLIND_INDEX_PEPPER` |
| ٤ | تشغيل `scripts/encrypt_existing_pii.py` |
| ٥ | إعادة توليد `ADMIN_PASS_HASH` بـ Argon2id |
| ٦ | جدولة `backup.py create` يومياً و`verify` أسبوعياً |
| ٧ | تفعيل أرشفة WAL من لوحة المزوّد |
| ٨ | حفظ المفاتيح في خزنة منفصلة عن قاعدة البيانات |
| ٩ | مراجعة سجلّات الإقلاع: صفر `❌` |

### فجوات مقصودة — تحتاج قرارك

**الصلاحيات على بقية الوحدات.** طُبّقت على الرواتب والموارد البشرية
والمالية. التدبير والصيانة ونقاط البيع ما زالت مفتوحة لكل موظف مسجَّل.
النمط جاهز:

```python
dependencies=[Depends(require_permission("housekeeping"))]
```

تُركت لأن تحديد من يملك ماذا في التشغيل اليومي قرار تشغيلي، ولأن
إغلاقها بتخمين قد يُعطّل موظفين شرعيين.

**تدوير مفتاح التشفير.** البادئة `enc:v1:` مهيّأة للتدرّج لكن المنطق لم
يُكتب. غير عاجل ما دام المفتاح محفوظاً.

**عدّاد معدّل مشترك.** العدّاد داخل العملية الواحدة، فمع تعدّد النسخ يصير
الحدّ مضروباً في عددها. يحتاج Redis.

**تكامل خدمة إدارة مفاتيح (KMS).** المفتاح اليوم في متغيّر بيئة.

### خارج الكود

- فاتورة ZATCA المرحلة الثانية إن كان عملاؤك ملزمين بها (المرحلة الأولى قائمة)
- سياسة خصوصية وسياسة احتفاظ بالبيانات لنظام حماية البيانات الشخصية
- إجراء للاستجابة لطلبات أصحاب البيانات

---

## الالتزامات

```
757ddfb  feat(backup)     نسخ احتياطي باستعادة مُتحقَّق منها فعلياً
d0fa534  feat(schema)     client_id إلزامي في 60 جدولاً + كشف الصفوف اليتيمة
2fb6fa3  feat(ratelimit)  حماية كل مسارات الـ API + إصلاح نمو الذاكرة
2ac98b3  feat(branches)   عزل الفروع في طبقة قاعدة البيانات
a259f64  feat(rbac)       دخول الموظفين وتطبيق الصلاحيات على المسارات
cbd4954  feat(audit)      سجل مراجعة يُكتب فيه فعلاً
f854b4e  fix(auth)        حماية دخول مالك المنصة من التخمين
f0b4ab1  fix(api)         إصلاح إضافة النزيل ونقاط الولاء + تحقّق المدخلات
43433d7  fix(auth)        بقاء الجلسة + فكّ حجب سبعة مسارات عن المالك
a6c5588  fix(api)         مسح شامل لكل مسار GET — تعارض مخطط سجل القنوات
c57873f  fix(api)         إصلاح أربعة مسارات تُعيد 500 في كل استدعاء
e774719  docs             ملحق الأمان والتوسّع + مصفوفة CI للعزل
3015a65  feat(reports)    9 طرق عرض تقريرية + توثيق داخل قاعدة البيانات
49753e9  feat(pii)        تشفير أرقام الهوية مع الإبقاء على البحث
5092128  feat(auth)       Argon2id + إصلاح خروج المنشأة عند إعادة التعيين
c90a499  feat(rls)        عزل مستأجرين حقيقي على مستوى قاعدة البيانات
4ebc8c5  fix(db)          تشغيل التحصين الأمني فعلياً
```

---

## الوثائق

| الملف | المحتوى |
|---|---|
| [`README.md`](README.md) | التشغيل ومتغيّرات البيئة وطبقات العزل |
| [`specs/db/06-security-and-scale.md`](specs/db/06-security-and-scale.md) | الأمان والتشفير والتوسّع واصطلاحات التسمية |
| [`specs/db/hotel-system-complete-model.md`](specs/db/hotel-system-complete-model.md) | نموذج البيانات — ١٥ قسماً |
| [`specs/db/04-isolation-hardening.sql`](specs/db/04-isolation-hardening.sql) | دوال وجداول العزل |
| [`specs/db/05-reporting-views.sql`](specs/db/05-reporting-views.sql) | طرق العرض التقريرية |

### أدوات تشغيلية

```bash
python3 -m db.crypto                          # توليد مفاتيح التشفير
python3 scripts/gen_admin_hash.py             # كلمة مرور المالك
python3 scripts/encrypt_existing_pii.py       # ترحيل أرقام الهوية
python3 scripts/find_orphan_rows.py           # الصفوف بلا منشأة
python3 scripts/backup.py create | verify     # النسخ الاحتياطي
```
